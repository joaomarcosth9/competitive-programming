#include <bits/stdc++.h>

#define _                                                                                                              \
    ios_base::sync_with_stdio(0);                                                                                      \
    cin.tie(0);
#define endl '\n'

using namespace std;

string t;
int n;

void solve() {
    cin >> t >> n;
    int len = t.size();
    vector<string> ss;
    for (auto &s : ss) cin >> s;
    int ls = -1;
    int lf = -1;
    int minsize = -1e9;
    while (lf != len - 1) {
    }
}

int main() {
    _ int tscf;
    cin >> tscf;
    while (tscf--) solve();
    return 0;
}
