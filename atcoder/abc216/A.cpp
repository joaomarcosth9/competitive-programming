#include <bits/stdc++.h>
using namespace std;
#define endl '\n'

signed main() {
    cin.tie(0)->sync_with_stdio(0);
    int a, b; char c;
    cin >> a >> c >> b;
    cout << a;
    if (b <= 2) cout << "-";
    else if (b <= 6);
    else cout << "+";
    cout << endl;
    return 0;
}
