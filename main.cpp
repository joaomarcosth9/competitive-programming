#include <bits/stdc++.h>

using namespace std;

void solve(){
}

signed main(){
#ifdef LOCAL_DEBUG
    freopen("/tmp/input.txt", "r", stdin);
    freopen("/tmp/output.txt", "w", stdout);
#else
    ios_base::sync_with_stdio(0);
    cin.tie(0);
#endif
    int tt; cin >> tt;
    while(tt--)
    	solve();
    return 0;
}
