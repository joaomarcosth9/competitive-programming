#include <bits/stdc++.h>

using namespace std;

void solve(){
}

int main(){
#ifdef SUBLIME
	freopen("/tmp/input.txt", "r", stdin);
	freopen("/tmp/output.txt", "w", stdout);
#endif
#ifndef LOCAL_DEBUG
	ios_base::sync_with_stdio(0);
	cin.tie(0);
#endif
	int t; cin >> t;
	while(t--)
	solve();
	return 0;
}
