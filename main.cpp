#include <bits/stdc++.h>

using namespace std;

void solve(){
}

int main(){
#ifdef SUBLIME
	freopen("/inp-out/input.txt", "r", stdin);
	freopen("/inp-out/output.txt", "w", stdout);
#endif
#ifndef LOCAL_DEBUG
	ios_base::sync_with_stdio(0);
	cin.tie(0);
#endif
	int t; cin >> t;
	while(t--)
	solve();
	return 0;
}
