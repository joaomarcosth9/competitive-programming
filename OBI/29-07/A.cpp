#include <bits/stdc++.h>

#define _                                                                                                              \
    ios_base::sync_with_stdio(0);                                                                                      \
    cin.tie(0);
#define endl '\n'

using namespace std;
const int MAX = 20;
int n, m, a, b, x, y;

void solve() { cin >> n >> m >> a >> b >> x >> y; }

int main() {
    _ solve();
    return 0;
}
