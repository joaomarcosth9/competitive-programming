#include <bits/stdc++.h>

using namespace std;
typedef long long ll;

const int MAX = 2.1e5;
int n, sq;
int arr[MAX];
int b[MAX];
int frente[MAX], tras[MAX];

void sum_left(int i, int v){

}

void sum_right(int i, int v){
    
}

void solve(){
    cin >> n;
    sq = (int)sqrt(n);
    int q; cin >> q; while(q--){
        int tq; cin >> tq;
        if(tq == 1){
        } else if (tq == 2){
        } else if (tq == 3){
        }
    }
}

signed main(){
    //ios_base::sync_with_stdio(0); cin.tie(0);
    solve();
    return 0;
}
